// TODO - check if logged in. if not then redirect to homepage

export default function AdminPage() {
  return (
    <main className="min-h-screen w-full bg-gray-50">
      <div className="max-w-4xl mx-auto p-4">Test some content here</div>
    </main>
  );
}
